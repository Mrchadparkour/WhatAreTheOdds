import React from 'react';
import { Alert, StyleSheet, Text, View } from 'react-native';
import MessageSlider from './src/components/MessageSlider';
//import component to be worked on up here first ^

export default class App extends React.Component {
  render() {
    //We will add React router up here later

    //Specify what component is being rendered here 
    return <MessageSlider />;
  }
}
